name: masjidconnect
description: "Masjid Finder App"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^3.1.0
  cloud_firestore: ^5.0.0
  
  # Updated dependencies - FIXED VERSION
  carousel_slider: ^5.1.1
  # REMOVED: wakelock_plus - not needed for your current code
  intl: ^0.18.1
  geolocator: ^11.1.0  
  http: ^1.1.0
  permission_handler: ^11.4.0  # Your current version
  url_launcher: ^6.1.10

  # Persistent storage (used for TV pairing persistence)
  shared_preferences: ^2.2.2
  
  # Add flutter_web_plugins for proper web support
  flutter_web_plugins:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/